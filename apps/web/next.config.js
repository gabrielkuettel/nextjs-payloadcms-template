module.exports = {
  reactStrictMode: true,
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'earth-protect.payloadcms.app'
      }
    ],
    domains: ['localhost', 'tailwindui.com']
  }
}
